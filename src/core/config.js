const config = {
	baseUrl: 'http://localhost:5000',
	events: {
		start: { label: 'Start' },
		stop: { label: 'Stop' },
		pause: { label: 'Pause' },
		resume: { label: 'Resume' },
	},
};

export default config;
